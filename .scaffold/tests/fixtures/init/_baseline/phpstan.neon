##
# Configuration file for PHPStan static code checking, see https://phpstan.org .
#
# Drupal-specific rules are included automatically by
# phpstan/extension-installer.
# @see https://github.com/mglaman/phpstan-drupal
#
# Paths are declared in the `paths` key below, not passed as CLI
# arguments.

parameters:

  level: 7

  # PHPStan caches reflection data that embeds absolute paths into its own
  # PHAR. The default cache directory is shared by every project on the
  # machine, so entries left by a build that no longer exists break the next
  # run.
  tmpDir: .phpstan

  paths:
    - web/modules/custom
    - web/themes/custom

  excludePaths:
    - vendor/*
    - node_modules/*
    - ../*/node_modules/*

  ignoreErrors:
    -
      # Hook implementations do not provide docblocks for parameters, so there
      # is no way to provide this information.
      messages:
        - '#.* no value type specified in iterable type array#'
        - '#.* has no return type specified#'
      reportUnmatched: false

  reportUnmatchedIgnoredErrors: false
